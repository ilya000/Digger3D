{
  "name": "digger-3d",
  "private": true,
  "version": "0.1.0",
  "license": "GPL-2.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && npm run build:worker",
    "preview": "vite preview",
    "test": "vitest run",
    "build:worker": "esbuild functions/_worker.src.js --bundle --format=esm --target=es2022 --outfile=dist/_worker.js"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "@types/three": "^0.186.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  }
}
